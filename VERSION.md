# MoneyTail5

Tagged release: `v5.0.0` / תג `MoneyTail5`

## Highlights
- Journey framing: track → patterns → next step
- Layered Clarity UI language across web + Android shell
- MonthFacts as single source of truth for monthly numbers
- Checking balance vs leftover vocabulary
- Goal funding separated from living expenses; net after goals
- Hebrew-first cashflow → leftover → goals

## Rollback
```bash
git checkout v5.0.0
```

Earlier checkpoint before trust work: `checkpoint-before-trust-fixes`
